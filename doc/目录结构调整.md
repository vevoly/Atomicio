
### 第一次架构调整
```
Atomicio/
├── pom.xml                 <-- [1. 根 POM] (聚合 <module>java</module>)
├── protocol/
│   └── protobuf/
└── java/
    ├── pom.xml             <-- [2. Java 父 POM] (聚合 <module>server</module>, <module>client</module>, <module>common</module>)
    ├── server/
    │   ├── pom.xml         <-- [3. Server 父 POM] (聚合 api, core, starter, example)
    │   ├── atomicio-api/
    │   │   └── pom.xml
    │   ├── atomicio-core/
    │   │   └── pom.xml
    │   ├── atomicio-spring-boot-starter/
    │   │   └── pom.xml
    │   └── atomicio-example/
    │       └── pom.xml
    ├── client/
    │   ├── pom.xml         <-- [4. Client 父 POM] (聚合 client-api, client-core)
    │   ├── atomicio-client-api/
    │   │   └── pom.xml
    │   └── atomicio-client-core/
    │       └── pom.xml
    └── common/
        ├── pom.xml         <-- [5. Common 父 POM] (聚合 codec)
        ├── atomicio-codec/
        │   ├── pom.xml     <-- [6. Codec 父 POM] (聚合 codec-text, codec-protobuf)
        │   ├── atomicio-codec-text/
        │   │   └── pom.xml
        │   └── atomicio-codec-protobuf/
        │        └── pom.xml
        └── atomicio-protocol-api/
             └── pom.xml    <-- 服务器端、客户端都会用到到协议 api
```

### 0.6.0 第二次架构调整
```

```